html {
  font-size: 14px;
}

@media (min-width: 768px) {
  html {
    font-size: 16px;
  }
}

:root {
  /* Paleta verde oscuro */
  --tn-bg: #0b2b18; /* fondo principal muy oscuro */
  --tn-surface: #0f3a23; /* superficies (navbar, header) */
  --tn-primary: #1f7a45; /* color principal */
  --tn-primary-contrast: #eaf7ef; /* texto claro sobre primary */
  --tn-accent: #2fb65a; /* acento para links y hover */
  --tn-muted: #9dbba9; /* textos secundarios */
  --tn-border: #244c36; /* bordes */
  --tn-danger: #a33a3a;
  --tn-focus-shadow: 0 0 0 0.1rem rgba(47,182,90,0.25), 0 0 0 0.25rem rgba(31,122,69,0.12);
}

html {
  position: relative;
  min-height: 100%;
}

html {
  font-size: 14px;
}

@media (min-width: 768px) {
  html {
    font-size: 16px;
  }
}

body {
  margin-bottom: 60px;
  background-color: var(--tn-bg);
  color: var(--tn-primary-contrast);
}

/* Enlaces y navegación */
a, .nav-link {
  color: var(--tn-accent);
}
a:hover, .nav-link:hover {
  color: #86e8a0;
  text-decoration: underline;
}

/* Navbar y encabezados */
.navbar, .bg-light {
  background-color: var(--tn-surface) !important;
  border-bottom: 1px solid var(--tn-border);
}
.navbar .nav-link, .navbar-brand, .bg-light * {
  color: var(--tn-primary-contrast) !important;
}

/* Botones */
.btn-primary {
  background-color: var(--tn-primary);
  border-color: var(--tn-border);
  color: var(--tn-primary-contrast);
}
.btn-primary:hover {
  background-color: var(--tn-accent);
  border-color: darken(var(--tn-accent), 5%);
}
.btn-outline-primary {
  color: var(--tn-primary);
  border-color: var(--tn-primary);
}
.btn-outline-primary:hover {
  background-color: rgba(47,182,90,0.08);
}

/* Inputs y foco accesible */
.btn:focus, .btn:active:focus, .btn-link.nav-link:focus, .form-control:focus, .form-check-input:focus {
  box-shadow: var(--tn-focus-shadow);
  border-color: var(--tn-primary);
}

.form-control, .form-select, textarea {
  background-color: #fff;
  color: #0a0a0a;
  border: 1px solid var(--tn-border);
}

.form-control::placeholder {
  color: var(--tn-muted);
}

/* Tarjetas y superficies */
.card {
  background-color: #0f3a23;
  color: var(--tn-primary-contrast);
  border: 1px solid var(--tn-border);
}

/* Tablas */
.table thead {
  background-color: var(--tn-surface);
  color: var(--tn-primary-contrast);
}
.table tbody tr {
  background-color: transparent;
  border-bottom: 1px solid rgba(255,255,255,0.04);
}

/* Footer fijo en la parte baja */
footer {
  background-color: var(--tn-surface);
  color: var(--tn-primary-contrast);
  border-top: 1px solid var(--tn-border);
}

/* Validación */
.text-danger {
  color: var(--tn-danger) !important;
}

.form-floating > .form-control-plaintext::placeholder, .form-floating > .form-control::placeholder {
  color: var(--tn-muted);
  text-align: end;
}

.form-floating > .form-control-plaintext:focus::placeholder, .form-floating > .form-control:focus::placeholder {
  text-align: start;
}

/* Ajustes pequeños para contraste en elementos claros */
.bg-white, .table, .container, .card-body {
  background-color: #082715;
}

/* Formularios: forma y distribución */
/* Contenedor centrado y con apariencia de tarjeta */
.tn-form {
  max-width: 920px;
  margin: 1.5rem auto;
  padding: 1.25rem 1.5rem;
  background: linear-gradient(180deg, rgba(15,58,35,0.95), rgba(11,43,24,0.95));
  border: 1px solid var(--tn-border);
  border-radius: 12px;
  box-shadow: 0 6px 18px rgba(0,0,0,0.45);
}

/* Grid responsive: una columna en móviles, dos columnas en ancho >=768px */
.tn-form .form-grid {
  display: grid;
  grid-template-columns: 1fr;
  gap: 1rem 1.25rem;
}
@media (min-width: 768px) {
  .tn-form .form-grid {
    grid-template-columns: 1fr 1fr;
  }
}

/* Asegura que los elementos que deben ocupar toda la fila lo hagan */
.tn-form .full-row {
  grid-column: 1 / -1;
}

/* Estética de campos: más altos, bordes redondeados y separación clara */
.tn-form label {
  display: block;
  margin-bottom: 0.35rem;
  color: var(--tn-primary-contrast);
  font-weight: 600;
  font-size: 0.95rem;
}

.tn-form .form-control,
.tn-form .form-select,
.tn-form textarea {
  height: 44px;
  padding: 0.5rem 0.75rem;
  border-radius: 10px;
  background-color: #ffffff;
  color: #0a0a0a;
  border: 1px solid var(--tn-border);
  transition: box-shadow .12s ease, transform .06s ease;
}

.tn-form textarea {
  min-height: 110px;
  height: auto;
  padding-top: 0.6rem;
}

.tn-form .form-control:focus,
.tn-form .form-select:focus,
.tn-form textarea:focus {
  outline: none;
  transform: translateY(-1px);
  box-shadow: var(--tn-focus-shadow);
  border-color: var(--tn-accent);
}

/* Agrupaciones */
.tn-form .form-group {
  margin-bottom: 0.9rem;
}

/* Checkbox y radio estilizados */
.tn-form .form-check {
  display: flex;
  align-items: center;
  gap: 0.6rem;
}
.tn-form .form-check-input {
  width: 1.25rem;
  height: 1.25rem;
  border-radius: 6px;
  border: 1px solid var(--tn-border);
}

/* Acciones del formulario */
.tn-form .form-actions {
  display: flex;
  justify-content: flex-end;
  gap: 0.75rem;
  margin-top: 0.5rem;
}

.tn-form .btn {
  border-radius: 10px;
  padding: 0.55rem 1rem;
  font-weight: 600;
}

.tn-form .btn-primary {
  background: linear-gradient(180deg, var(--tn-primary), var(--tn-accent));
  border: 1px solid rgba(0,0,0,0.15);
  color: var(--tn-primary-contrast);
}

.tn-form .btn-secondary {
  background: transparent;
  color: var(--tn-primary-contrast);
  border: 1px solid rgba(255,255,255,0.06);
}

/* Mensajes de ayuda y validación */
.tn-form .form-text {
  color: var(--tn-muted);
  font-size: 0.85rem;
}

.tn-form .is-invalid {
  border-color: rgba(163,58,58,0.9);
  box-shadow: 0 0 0 0.12rem rgba(163,58,58,0.12);
}

/* Mejora de espaciado en formularios dentro de tablas o listados */
form .row > .col-md-6,
form .row > .col-sm-6 {
  margin-bottom: 0.5rem;
}
